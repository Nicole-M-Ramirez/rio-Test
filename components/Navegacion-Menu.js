import { StyleSheet, Text, View, Button } from 'react-native';

function NavegacionMenu({opcion}) {
    return(
        <View>
            <Text>Navegacion Menu</Text>
        </View>
    );
}

export default NavegacionMenu;